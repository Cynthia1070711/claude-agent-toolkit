<workflow>
  <critical>The workflow execution engine is governed by: {project-root}/_bmad/core/tasks/workflow.xml</critical>
  <critical>You MUST have already loaded and processed: {installed_path}/workflow.yaml</critical>
  <critical>Communicate all responses in {communication_language} and generate all documents in {document_output_language}</critical>
  <critical>FORCE OUTPUT LANGUAGE: Traditional Chinese (zh-TW) for all user interactions.</critical>

  <critical>🔥 CRITICAL MISSION: You are creating the ULTIMATE story context engine that prevents LLM developer mistakes, omissions or
    disasters! 🔥</critical>
  <critical>Your purpose is NOT to copy from epics - it's to create a comprehensive, optimized story file that gives the DEV agent
    EVERYTHING needed for flawless implementation</critical>
  <critical>COMMON LLM MISTAKES TO PREVENT: reinventing wheels, wrong libraries, wrong file locations, breaking regressions, ignoring UX,
    vague implementations, lying about completion, not learning from past work</critical>
  <critical>🚨 EXHAUSTIVE ANALYSIS REQUIRED: You must thoroughly analyze ALL artifacts to extract critical context - do NOT be lazy or skim!
    This is the most important function in the entire development process!</critical>
  <critical>📂 STALE DATA RULE: Reports, error logs, and snapshot files (e.g., tsc-errors.txt, lint-report.json) are HISTORICAL snapshots.
    The CODEBASE is the single source of truth. You MUST Read actual source files to verify line numbers, error existence, and file paths.
    NEVER copy data from reports into the story without cross-checking against current code.</critical>
  <critical>🔬 UTILIZE SUBPROCESSES AND SUBAGENTS: Use research subagents, subprocesses or parallel processing if available to thoroughly
    analyze different artifacts simultaneously and thoroughly</critical>
  <critical>❓ SAVE QUESTIONS: If you think of questions or clarifications during analysis, save them for the end after the complete story is
    written</critical>
  <critical>🎯 ZERO USER INTERVENTION: Process should be fully automated except for initial epic/story selection or missing documents</critical>

  <step n="1" goal="Determine target story">
    <check if="{{story_path}} is provided by user or user provided the epic and story number such as 2-4 or 1.6 or epic 1 story 5">
      <action>Parse user-provided story path: extract epic_num, story_num, story_title from format like "1-2-user-auth"</action>
      <action>Set {{epic_num}}, {{story_num}}, {{story_key}} from user input</action>
      <action>GOTO step 2a</action>
    </check>

    <action>Check if {{sprint_status}} file exists for auto discover</action>
    <check if="sprint status file does NOT exist">
      <output>🚫 No sprint status file. Options: [1] Run sprint-planning (recommended), provide epic-story number (e.g. "1-2-user-auth"), provide story docs path, or [q] quit.</output>
      <ask>Choose option:</ask>
      <check if="user chooses 'q'"><action>HALT</action></check>
      <check if="user chooses '1'">
        <output>Run sprint-planning first.</output>
        <action>HALT</action>
      </check>
      <check if="user provides epic-story number">
        <action>Parse user input: extract epic_num, story_num, story_title</action>
        <action>Set {{epic_num}}, {{story_num}}, {{story_key}} from user input</action>
        <action>GOTO step 2a</action>
      </check>
      <check if="user provides story docs path">
        <action>Use user-provided path for story documents</action>
        <action>GOTO step 2a</action>
      </check>
    </check>

    <!-- Auto-discover from sprint status only if no user input -->
    <check if="no user input provided">
      <critical>MUST read COMPLETE {sprint_status} file from start to end to preserve order</critical>
      <action>Load the FULL file: {{sprint_status}}</action>
      <action>Store loaded content as {{sprint_status_cache}} for later use in Step 7 — do NOT re-read the file in subsequent steps</action>
      <action>Read ALL lines from beginning to end - do not skip any content</action>
      <action>Parse the development_status section completely</action>

      <action>Find the FIRST story (by reading in order from top to bottom) where:
        - Key matches pattern: number-number-name (e.g., "1-2-user-auth")
        - NOT an epic key (epic-X) or retrospective (epic-X-retrospective)
        - Status value equals "backlog"
      </action>

      <check if="no backlog story found">
        <output>📋 No backlog stories in sprint-status.yaml. Options: run sprint-planning, correct-course, or check if sprint is complete.</output>
        <action>HALT</action>
      </check>

      <action>Extract from found story key (e.g., "1-2-user-authentication"):
        - epic_num: first number before dash (e.g., "1")
        - story_num: second number after first dash (e.g., "2")
        - story_title: remainder after second dash (e.g., "user-authentication")
      </action>
      <action>Set {{story_id}} = "{{epic_num}}.{{story_num}}"</action>
      <action>Store story_key for later use (e.g., "1-2-user-authentication")</action>

      <!-- Mark epic as in-progress if this is first story -->
      <action>Check if this is the first story in epic {{epic_num}} by looking for {{epic_num}}-1-* pattern</action>
      <check if="this is first story in epic {{epic_num}}">
        <action>Use {{sprint_status_cache}} (loaded above) to check epic-{{epic_num}} status — do NOT re-read the file</action>
        <action>If epic status is "backlog" → update to "in-progress"</action>
        <action>If epic status is "contexted" (legacy status) → update to "in-progress" (backward compatibility)</action>
        <action>If epic status is "in-progress" → no change needed</action>
        <check if="epic status is 'done'">
          <output>🚫 ERROR: Epic {{epic_num}} is done. Change to 'in-progress' in sprint-status.yaml or create a new epic.</output>
          <action>HALT</action>
        </check>
        <check if="epic status is not one of: backlog, contexted, in-progress, done">
          <output>🚫 ERROR: Invalid epic status '{{epic_status}}' for epic {{epic_num}}. Fix sprint-status.yaml or run sprint-planning.</output>
          <action>HALT</action>
        </check>
        <output>📊 Epic {{epic_num}} status updated to in-progress</output>
      </check>

      <action>GOTO step 2a</action>
    </check>
  </step>

  <step n="2" goal="Load and analyze core artifacts">
    <critical>🔬 EXHAUSTIVE ARTIFACT ANALYSIS - This is where you prevent future developer fuckups!</critical>

    <!-- Load all available content through discovery protocol -->
    <invoke-protocol
      name="discover_inputs" />
    <note>Available content: {epics_content}, {prd_content}, {architecture_content}, {ux_content},
    {project_context}</note>

    <!-- Analyze epics file for story foundation -->
    <action>From {epics_content}, extract Epic {{epic_num}} complete context:</action> **EPIC ANALYSIS:** - Epic
    objectives and business value - ALL stories in this epic for cross-story context - Our specific story's requirements, user story
    statement, acceptance criteria - Technical requirements and constraints - Dependencies on other stories/epics - Source hints pointing to
    original documents <!-- Extract specific story requirements -->
    <action>Extract our story ({{epic_num}}-{{story_num}}) details:</action> **STORY FOUNDATION:** - User story statement
    (As a, I want, so that) - Detailed acceptance criteria (already BDD formatted) - Technical requirements specific to this story -
    Business context and value - Success criteria <!-- Previous story analysis for context continuity -->
    <check if="story_num > 1">
      <action>Load previous story file: {{story_dir}}/{{epic_num}}-{{previous_story_num}}-*.md</action> **PREVIOUS STORY INTELLIGENCE:** -
    Dev notes and learnings from previous story - Review feedback and corrections needed - Files that were created/modified and their
    patterns - Testing approaches that worked/didn't work - Problems encountered and solutions found - Code patterns established <action>Extract
    all learnings that could impact current story implementation</action>
    </check>

    <!-- Git intelligence for previous work patterns -->
    <check
      if="previous story exists AND git repository detected">
      <action>Get last 5 commit titles to understand recent work patterns</action>
      <action>Analyze 1-5 most recent commits for relevance to current story:
        - Files created/modified
        - Code patterns and conventions used
        - Library dependencies added/changed
        - Architecture decisions implemented
        - Testing approaches used
      </action>
      <action>Extract actionable insights for current story implementation</action>
    </check>
  </step>

  <step n="3" goal="Codebase analysis for story context verification">
    <critical>🔍 CODEBASE REALITY CHECK - Verify story tasks against actual implementation!</critical>
    <critical>This step PREVENTS: duplicate work, missed existing abstractions, wrong file locations, breaking regressions</critical>
    <mandate>🚫 GATE: DO NOT proceed to Step 4 until EVERY source file mentioned in the story has been READ and verified.
      Reports, error logs, and snapshot files are HISTORICAL DATA — the codebase is the SINGLE SOURCE OF TRUTH.
      Any line number, error count, or file path from reports MUST be cross-checked against the actual code.</mandate>

    **STALE DATA DETECTION (MANDATORY):**
    <mandate>Before trusting ANY data file (error logs, tsc-errors.txt, lint reports, test results):
      1. Check file modification date — if older than latest git commits, it is STALE
      2. If stale → Note "⚠️ 快照過期" and verify each claim against actual source code
      3. NEVER copy line numbers from reports without opening the source file to confirm
      4. If story references specific line numbers → you MUST Read those lines and verify</mandate>

    **IDENTIFY TARGET CODE AREAS:**
    <action>From story requirements, extract keywords for code search:
      - Component names, service names, hook names mentioned
      - Feature areas (e.g., "payment", "authentication", "editor")
      - Data models and entity names
      - API endpoint patterns
    </action>

    **SCAN EXISTING IMPLEMENTATION:**
    <action>Use Glob/Grep to find relevant existing files:
      - Frontend: src/components/**, src/hooks/**, src/services/**, src/stores/**
      - Backend: Controllers/**, Services/**, Models/**
      - Database: Migrations/**, Data/**
    </action>

    **MANDATORY SOURCE FILE VERIFICATION:**
    <mandate>For EVERY file mentioned in story requirements or error reports:
      1. Read the actual source file at the referenced line numbers
      2. Confirm the error/issue described actually exists at that location
      3. If line number is wrong → find the correct line and update
      4. If error no longer exists → mark as "已修復" and update story
      5. Use subagents/parallel reads for efficiency when verifying multiple files</mandate>

    <action>Read and analyze found files to understand:
      - Current implementation state
      - Existing patterns and abstractions
      - Code conventions in use
      - Related functionality already built
    </action>

    **COMPARE STORY TASKS VS CODEBASE:**
    <action>For each task/subtask in the story, verify against codebase:
      - Does the target file already exist? → Note "修改" vs "新增"
      - Is there partial implementation? → Note completion percentage
      - Are there existing utilities/hooks to reuse? → List them
      - Are there conflicting implementations? → Flag for resolution
    </action>

    **STORE CODEBASE FINDINGS:**
    <action>Create {{codebase_analysis}} with:
      - existing_files: List of relevant files found with their purposes
      - reusable_abstractions: Existing hooks, services, utilities to leverage
      - partial_implementations: Tasks that have some code already
      - conflicts: Any code that may conflict with story requirements
      - recommended_approach: Based on actual code state
      - stale_data_corrections: Any report data that was outdated and corrected
    </action>

    <check if="significant existing implementation found">
      <action>Update story tasks to reflect reality:
        - Mark partially done tasks with "已有部分實作: [file path]"
        - Add notes about existing code to reuse
        - Adjust complexity if work is less than expected
      </action>
    </check>

    **VERIFICATION SUMMARY (MANDATORY OUTPUT):**
    <mandate>Before proceeding to Step 4, output a verification summary:
      - Total source files checked: N
      - Confirmed issues: N (line numbers verified)
      - Stale/incorrect data corrected: N (list each correction)
      - Files not found or inaccessible: N
      This summary MUST appear in the story's "程式碼現況分析" section.</mandate>
  </step>

  <step n="4" goal="Architecture analysis for developer guardrails">
    <critical>🏗️ ARCHITECTURE INTELLIGENCE - Extract everything the developer MUST follow!</critical> **ARCHITECTURE DOCUMENT ANALYSIS:** <action>Systematically
    analyze architecture content for story-relevant requirements:</action>

    <!-- Load architecture - single file or sharded -->
    <check if="architecture file is single file">
      <action>Load complete {architecture_content}</action>
    </check>
    <check if="architecture is sharded to folder">
      <action>Load architecture index and scan all architecture files</action>
    </check> **CRITICAL ARCHITECTURE EXTRACTION:** <action>For
    each architecture section, determine if relevant to this story:</action> - **Technical Stack:** Languages, frameworks, libraries with
    versions - **Code Structure:** Folder organization, naming conventions, file patterns - **API Patterns:** Service structure, endpoint
    patterns, data contracts - **Database Schemas:** Tables, relationships, constraints relevant to story - **Security Requirements:**
    Authentication patterns, authorization rules - **Performance Requirements:** Caching strategies, optimization patterns - **Testing
    Standards:** Testing frameworks, coverage expectations, test patterns - **Deployment Patterns:** Environment configurations, build
    processes - **Integration Patterns:** External service integrations, data flows <action>Extract any story-specific requirements that the
    developer MUST follow</action>
    <action>Identify any architectural decisions that override previous patterns</action>
  </step>

  <step n="5" goal="Web research for latest technical specifics">
    <critical>🌐 ENSURE LATEST TECH KNOWLEDGE - Prevent outdated implementations!</critical> **WEB INTELLIGENCE:** <action>Identify specific
    technical areas that require latest version knowledge:</action>

    <!-- Check for libraries/frameworks mentioned in architecture -->
    <action>From architecture analysis, identify specific libraries, APIs, or
    frameworks</action>
    <action>For each critical technology, research latest stable version and key changes:
      - Latest API documentation and breaking changes
      - Security vulnerabilities or updates
      - Performance improvements or deprecations
      - Best practices for current version
    </action>
    **EXTERNAL CONTEXT INCLUSION:** <action>Include in story any critical latest information the developer needs:
      - Specific library versions and why chosen
      - API endpoints with parameters and authentication
      - Recent security patches or considerations
      - Performance optimization techniques
      - Migration considerations if upgrading
    </action>
  </step>

  <step n="6" goal="Create comprehensive story file">
    <critical>📝 CREATE ULTIMATE STORY FILE - The developer's master implementation guide!</critical>

    <action>Set {{epic_num}} = extracted epic_num</action>
    <action>Initialize from template.md:
    {implementation_artifacts}/stories/epic-{{epic_num}}/{{story_key}}.md</action>

    <!-- SKILLS ANALYSIS - 分析並註記需要的 Skills -->
    <critical>🔧 REQUIRED SKILLS ANALYSIS - 參照 Skills 清單分析此 Story 需要哪些技術規範！</critical>
    <action>Load Skills catalog from: {project-root}/.claude/skills/skills_list.md</action>
    <action>Match story content keywords against trigger keywords in the loaded Skills catalog.
      Do NOT hardcode skill mappings — skills_list.md is the single source of truth.</action>
    <action>For each matched Skill, note WHY it's needed based on story content</action>
    <action>Store matched Skills as {{required_skills}} list</action>

    <!-- CRITICAL: Fill Story 資訊 metadata table -->
    <critical>🏷️ STORY METADATA - Must fill ALL fields in ## Story 資訊 table!</critical>
    <action>Extract and fill Story 資訊 table fields:
      - {{story_key}}: Full story identifier (e.g., "bf-26-1-payment-deadline-tab")
      - {{epic_name}}: Epic title from epics document (e.g., "Epic BugFix: 手動審查問題修復")
      - {{priority}}: P0/P1/P2/P3 from epics or PRD
      - {{story_type}}: Feature / Tech Debt / Bug Fix / Enhancement
      - {{complexity}}: XS (1 SP) / S (2 SP) / M (3-5 SP) / L (8 SP) / XL (13+ SP)
      - {{source_reference}}: Origin of this story (e.g., "PRD Section 3.2" or "BF-20 Code Review")
      - {{dependencies}}: Other stories this depends on (e.g., "BF-26-1 (已完成)")
      - {{date}}: Current system date (YYYY-MM-DD format)
    </action>
    <action>Fill Agent tracking fields in Story 資訊 table:
      - Create Agent: Record the current LLM model name and version (e.g., "Claude Opus 4.6", "Claude Sonnet 4.6", "Gemini 3.1 Pro")
      - Create完成時間: Execute `powershell -Command "Get-Date -Format 'yyyy-MM-dd HH:mm'"` to get current time
      - DEV Agent / DEV完成時間 / Review Agent / Review完成時間: Set to "—" (filled by dev-story and code-review workflows)
    </action>
    <critical>NEVER leave placeholder text like {{variable}} in final output - all must be replaced with actual values!</critical>

    <template-output file="{implementation_artifacts}/stories/epic-{{epic_num}}/{{story_key}}.md">story_header</template-output>

    <!-- Story foundation from epics analysis -->
    <template-output
      file="{default_output_file}">story_requirements</template-output>

    <!-- REQUIRED SKILLS SECTION - 寫入分析出的 Skills -->
    <critical>📚 WRITE REQUIRED SKILLS - dev-story 和 code-review 會讀取此區塊載入 Skills！</critical>
    <action>Write "## Required Skills" section to story file with {{required_skills}}:
      ```markdown
      ## Required Skills

      > dev-story 和 code-review 執行時，必須先載入以下 Skills。

      {{#each required_skills}}
      - {{skill_name}} - {{reason}}
      {{/each}}
      ```
    </action>
    <check if="{{required_skills}} is empty">
      <action>Write note: "此 Story 無需特定技術規範 Skill。"</action>
    </check>

    <!-- SECTION-LEVEL REFERENCE RULE for Dev Notes -->
    <critical>📂 PATH REFERENCE RULE: All file references in Dev Notes MUST use full relative paths from project root.
      Functional spec references MUST include section number (e.g., §5.1) and mark "僅讀此節" to limit read scope.
      Format: | 類型 | `docs/.../XX.file.md` | §X.X | 僅讀此節 |
      NEVER use shorthand like "功能規格 #25 §5.1" — always resolve to actual file path.</critical>
    <critical>🔗 DEPENDENCY INTERFACE RULE: Story dependency field MUST include concrete interface/method names.
      Format: Story-ID（FileName.cs: MethodName() / InterfaceName）
      NEVER use vague descriptions like "可平行但建議一起".</critical>

    <!-- Developer context section - MOST IMPORTANT PART -->
    <template-output file="{default_output_file}">
    developer_context_section</template-output> **DEV AGENT GUARDRAILS:** <template-output file="{default_output_file}">
    technical_requirements</template-output>
    <template-output file="{default_output_file}">architecture_compliance</template-output>
    <template-output
      file="{default_output_file}">library_framework_requirements</template-output>
    <template-output file="{default_output_file}">
    file_structure_requirements</template-output>
    <template-output file="{default_output_file}">testing_requirements</template-output>

    <!-- Codebase analysis results - CRITICAL for preventing duplicate work -->
    <check if="{{codebase_analysis}} is available">
      <critical>🔍 CODEBASE ANALYSIS SECTION - Include findings from Step 3!</critical>
      <action>Write "## 程式碼現況分析" section to story file:
        ```markdown
        ## 程式碼現況分析

        > 此區塊由 create-story 自動分析程式碼後產生，協助開發者了解現有實作狀態。

        ### 相關現有檔案
        {{#each codebase_analysis.existing_files}}
        - `{{path}}` - {{purpose}}
        {{/each}}

        ### 可重用的抽象/工具
        {{#each codebase_analysis.reusable_abstractions}}
        - `{{name}}` ({{path}}) - {{description}}
        {{/each}}

        ### 已有部分實作的任務
        {{#each codebase_analysis.partial_implementations}}
        - **{{task}}**: {{status}} - 參考 `{{file}}`
        {{/each}}

        ### 注意事項
        {{#each codebase_analysis.conflicts}}
        - ⚠️ {{description}}
        {{/each}}

        ### 建議實作方式
        {{codebase_analysis.recommended_approach}}
        ```
      </action>
    </check>
    <check if="{{codebase_analysis}} is NOT available or empty">
      <action>Write note: "程式碼分析未發現相關現有實作，此為全新功能開發。"</action>
    </check>

    <!-- Previous story intelligence -->
    <check
      if="previous story learnings available">
      <template-output file="{default_output_file}">previous_story_intelligence</template-output>
    </check>

    <!-- Git intelligence -->
    <check
      if="git analysis completed">
      <template-output file="{default_output_file}">git_intelligence_summary</template-output>
    </check>

    <!-- Latest technical specifics -->
    <check if="web research completed">
      <template-output file="{default_output_file}">latest_tech_information</template-output>
    </check>

    <!-- Project context reference -->
    <template-output
      file="{implementation_artifacts}/stories/epic-{{epic_num}}/{{story_key}}.md">project_context_reference</template-output>

    <!-- Final status update -->
    <template-output file="{implementation_artifacts}/stories/epic-{{epic_num}}/{{story_key}}.md">
    story_completion_status</template-output>

    <!-- CRITICAL: Set status to ready-for-dev -->
    <action>Set story Status to: "ready-for-dev"</action>
    <action>Add completion note: "Ultimate
    context engine analysis completed - comprehensive developer guide created"</action>
  </step>

  <step n="7" goal="Update sprint status and finalize">
    <invoke-task>Validate against checklist at {installed_path}/checklist.md using _bmad/core/tasks/validate-workflow.xml</invoke-task>
    <action>Save story document unconditionally</action>

    <!-- Update tracking file if not already done in step 5 -->
    <action>Set {{tracking_active_path}} = "{output_folder}/tracking/active/{{story_key}}.track.md"</action>
    <check if="{{tracking_active_path}} does NOT exist">
      <action>Create tracking file from template:
        - 路徑: {{tracking_active_path}}
        - 狀態: ready-for-dev
      </action>
      <action>Update {output_folder}/tracking/README.md</action>
    </check>

    <!-- Update sprint status -->
    <check if="sprint status file exists">
      <action>Update {{sprint_status}}</action>
      <action>Use {{sprint_status_cache}} from Step 1 — do NOT re-read the file</action>
      <action>Find development_status key matching {{story_key}}</action>
      <action>Verify current status is "backlog" (expected previous state)</action>
      <action>Update development_status[{{story_key}}] = "ready-for-dev"</action>
      <action>Save file, preserving ALL comments and structure including STATUS DEFINITIONS</action>
    </check>

    <action>Report completion</action>
    <output>**🎯 Story Context 建立完成!**

      - **Story:** {{story_key}} | **狀態:** ready-for-dev
      - **檔案:** {{story_file}}
      - **建立:** {{current_date}} by {{user_name}} (AI-Assisted)

      **🔧 Required Skills:**
      {{#each required_skills}}
      - **{{skill_name}}**: {{reason}}
      {{/each}}
      <check if="{{required_skills}} is empty">
      - (無特定 Skill 需求)
      </check>

      **下一步:** 檢閱 Story → `dev-story` → `code-review`
    </output>
  </step>

</workflow>